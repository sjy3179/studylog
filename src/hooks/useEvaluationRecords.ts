import { useCallback,useEffect,useState } from 'react';import { evaluationRepository } from '@/data/repositories/EvaluationRepository';import type{EvaluationRecord}from '@/data/evaluation/evaluation-types';export function useEvaluationRecords(){const[data,setData]=useState<EvaluationRecord[]>([]);const reload=useCallback(async()=>setData(await evaluationRepository.list()),[]);useEffect(()=>{queueMicrotask(()=>void reload())},[reload]);return{data,reload}}
