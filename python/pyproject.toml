[project]
name = "studylog-analysis"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [".pytest-tmp"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
